// cat.js
 

export default class Cat {
  constructor(name) {
  	this.name = name;
  }

  meow() {
  	return "i am cat";
  	// var annimate=new Annimate("xiaoxi1");
   //  return `${this.name}: say ${annimate.meow()} `;
  }
}
